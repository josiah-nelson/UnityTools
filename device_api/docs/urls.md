# URL Catalog

A comprehensive URL list (paths) discovered from HAR/PCAP/JS artifacts is exported here:
- `output/urls/all_urls.tsv` (surface + methods)
- `output/urls/all_urls.txt` (paths only)
- `output/urls/urls_by_surface/*.txt`

